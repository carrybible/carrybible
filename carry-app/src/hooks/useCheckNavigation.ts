import { NavigationRoot } from '@scenes/root'

const useCheckNavigation = () => {
  const navigateToGroupHome = NavigationRoot.home

  return { navigateToGroupHome }
}

export default useCheckNavigation
